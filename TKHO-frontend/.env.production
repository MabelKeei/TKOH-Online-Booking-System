# 生产：JSON API 直连 ngrok（axios 可带 ngrok-skip-browser-warning）
VITE_API_ORIGIN=https://sampling-triceps-unbent.ngrok-free.dev

# 上传图片：前端 fetch + ngrok 头转 blob URL（见 apiAsset.js），勿用 Netlify _redirects 代理 uploads
# 勿设 VITE_USE_SAME_ORIGIN_API=true，否则整站 /api 经 Netlify 代理会拿到 ngrok HTML 导致表格无数据
